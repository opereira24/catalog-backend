# Base de dados (Neon PostgreSQL)
DB_URL=jdbc:postgresql://<host>.neon.tech/<database>?sslmode=require
DB_USERNAME=
DB_PASSWORD=

# Token partilhado com o dcbo-backend para autenticar o endpoint interno de sync
CATALOG_SYNC_TOKEN=

PORT=8081
